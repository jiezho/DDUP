{
  "name": "storage-client",
  "version": "1.0.0",
  "description": "MinIO 对象存储客户端，支持上传/下载/列出/预签名/删除。自动按实例 ID 命名空间隔离。",
  "author": "ddup-shared-library",
  "compatibility": "hermes >= 0.13.0",
  "dependencies": ["minio"],
  "env_vars": ["MINIO_ENDPOINT", "MINIO_BUCKET", "MINIO_ACCESS_KEY", "MINIO_SECRET_KEY", "HERMES_INSTANCE_ID"],
  "install_instructions": "pip install minio，将本目录复制到 skills/ 中",
  "tags": ["storage", "minio", "shared-library", "core"],
  "priority": "P0"
}
