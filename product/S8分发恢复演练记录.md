# S8 分发恢复演练记录

> 日期：2026-09-16
> 状态：恢复核心、容量失败保护、历史迁移和回退失败关闭完成；真实跨设备演练待完成

## 1. 本轮完成

- 恢复前完整验证 Manifest 格式、数据库条目、来源根和全部文件摘要。
- 拒绝路径逃逸、重复清单项、非普通文件、缺失数据库和不支持的 Manifest。
- 只恢复 Manifest 明确列出的文件，忽略包内未列出的附加文件。
- 在隔离暂存目录完成写入和 SQLite `quick_check` 后，再原子切换到最终目录。
- 失败时清理暂存目录；已有非空目标保持原样。
- 仍只允许停机恢复到空目录，不覆盖运行中的实例。
- 复制阶段发生错误时删除全部隔离暂存文件，不创建半成品目标目录。
- 两个恢复同时竞争同一目标时只允许一个原子发布；失败方清理自身暂存，不覆盖成功结果。
- 恢复前读取目标文件系统可用容量；除全部清单文件外，至少保留 64 MiB 或文件总量 5% 的较大者。
- 容量预检不足时在创建暂存前失败；写入期间发生 `ENOSPC` 时清理全部暂存且不发布目标。
- v14 历史数据库恢复后可原子升级到当前 v15；遇到当前应用无法识别的更高迁移版本时失败关闭，原项目数据和数据库完整性保持不变。

## 2. 自动验收

`tests/backup-recovery.test.mjs` 当前覆盖：

1. SQLite 对象、关系、来源文件和哈希成功恢复。
2. 摘要损坏在写入目标前失败。
3. 非空目标拒绝且原内容不变。
4. 路径逃逸和重复清单项拒绝。
5. 清单外文件不会进入恢复结果。
6. 容量预检不足与写入期间 `ENOSPC` 均不留下半成品。
7. 历史数据库升级成功；未知新版本拒绝启动且不改写原数据。

2026-09-16 专项结果：7/7 测试通过；连续重复 10 轮共 70/70 通过，容量、竞争和迁移用例未出现半成品、双成功、残留暂存或数据库损坏。完整回归 281/281、独立生产构建和隐私扫描通过；提交 `c40a21c` 的 GitHub Actions 7/7 通过，流水线 `35046187798`。

## 3. 剩余演练

- 从正式分发包生成备份，在另一台或干净虚拟机恢复。
- 使用真实受限虚拟磁盘复验预检与操作系统 `ENOSPC`；当前证据为确定性故障注入。
- 使用实际上一版本程序包复验“新数据库拒绝降级且不丢数据”；当前证据为未知迁移版本模拟。
- 在跨设备恢复后执行全量业务回归、隐私扫描和对象计数复核。

在以上项目完成前，S8-04 只能记为“本地恢复核心通过”，不能记为“跨设备分发恢复通过”。
