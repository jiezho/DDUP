# 旧版 XLSX 导入依赖处置

> 版本：V1.0
> 日期：2026-09-08
> 状态：已确认并实现
> 确认日期：2026-09-08
> 最终决定：X1、X2、X3、X4、X5 全部采用推荐项
> 触发原因：G3 后依赖审计发现原项目 `xlsx@0.18.5` 存在已公开的高风险安全告警，而 npm 注册表没有可直接升级的修复版本  
> 影响范围：仅现有 Vault 中旧版 XLSX 明细读取；不涉及新项目数据库、上传接口或真实资料接入

## 0. 已确认的决定

| 编号 | 最终决定 | 实施结果 | 验收证据 |
|---|---|---|---|
| X1 | 固定官方 `xlsx-0.20.3.tgz` | 已使用精确本地 `file:` 依赖 | `vendor/xlsx-0.20.3.tgz`、`package-lock.json` |
| X2 | 保存供应链证据 | 已记录来源、SHA-256、体积、许可证、包清单和安装脚本检查 | `vendor/xlsx-0.20.3.manifest.json` |
| X3 | 受控路径与资源预算 | 已增加 realpath allowlist、ZIP 预检、大小/展开量/条目/表/行/单元格限制 | `server/vault-xlsx-reader.mjs` |
| X4 | 不新增上传/API | 仅旧版 Vault 内部入口；测试只生成明确的 synthetic fixture | `tests/vault-xlsx-reader.test.mjs` |
| X5 | 任一安全门失败则禁用 | 本次所有门通过，未触发回退 | Node 24 测试、构建、隐私扫描、生产审计 |

## 1. 处置前事实与当前结果

- `xlsx` 是 G3 前已存在的直接生产依赖，处置前版本为 `0.18.5`；不是本轮新增。
- 代码只在 `server/vault-index.mjs` 中用于读取旧版 `.xlsx` 明细，并标记来源为 `official-xlsx-legacy`。
- 处置前 `npm audit` 剩余 1 个 high 级依赖问题，涉及原型污染和正则表达式拒绝服务；公告给出的修复版本分别为 `0.19.3` 和 `0.20.2`。
- npm 注册表中的 `xlsx` 仍停留在 `0.18.5`，因此 `npm audit fix` 无法修复。
- SheetJS 官方安装文档说明 npm 注册表副本已过时，当前官方发布渠道是其 CDN，并给出 `0.20.3` tarball；官方还建议为稳定构建保存本地副本。

`0.20.3` 已在本项目中完成下载、校验、安装和 Node 24 验证。

## 2. 推荐方案

### 2.1 固定和验证供应链

确认后执行以下受控流程：

1. 仅从 `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` 下载；
2. 将 tarball 保存到 Workbench 的 `vendor/`，不执行其中代码；
3. 记录 SHA-256、文件大小、许可证、package metadata、包内文件清单和安装脚本检查结果；
4. `package.json` 使用精确本地 `file:vendor/xlsx-0.20.3.tgz`，lock 不引用浮动版本；
5. 重新执行完整测试、构建、隐私扫描和生产依赖审计；
6. 审计必须不再出现该两项告警，否则回退并禁用功能。

本地 vendoring 仅固定一个已经批准的第三方制品，不授权二次分发。项目许可证和第三方 notices 未完成前仍保持内部本地开发状态。

### 2.2 解析边界

即使依赖升级，XLSX 仍按不可信输入处理：

- 入口限定为既有 Vault 受控目录，经 `realpath + allowlist` 校验；
- 单文件最大 20 MiB；
- ZIP 最多 10,000 个条目，解压后总量最多 100 MiB；
- 默认最多 32 个工作表、每表 100,000 行、总计 1,000,000 个有效单元格；
- 达到任何预算即失败并记录结构化错误，不产生部分或伪造结果；
- 不执行宏、外链、公式或 HTML；公式只按不可执行文本/缓存值处理；
- 解析异常不影响上一次可用索引；日志不记录单元格正文和本地私人路径；
- MVP 不通过 HTTP、消息连接器或 Runtime Tool 暴露通用 XLSX 解析。

工作表/行/单元格及 ZIP 展开预算已由回归测试固定；任何预算或容器校验失败均禁用该次旧版读取，不以“升级版本”代替隔离控制。

## 3. 备选方案比较

| 方案 | 功能保留 | 安全/审计 | 可复现性 | 结论 |
|---|---|---|---|---|
| A. 官方 0.20.3 tarball 本地固定 | 保留 | 有机会清除已公告问题，仍需边界测试 | 高 | **推荐** |
| B. 删除依赖并禁用旧版 XLSX | 不保留该旧入口 | 最小攻击面 | 高 | 可靠回退方案 |
| C. 继续使用 0.18.5 | 保留 | high 告警持续存在 | 中 | 不允许进入发布基线 |
| D. 使用其他解析库 | 需重新验证兼容 | 新依赖面和迁移风险 | 取决于库 | 当前没有足够收益，不推荐 |

## 4. 验收证据

- 官方 tarball 为 2,409,319 字节，SHA-256 为 `8dc73fc3b00203e72d176e85b50938627c7b086e607c682e8d3c22c02bb99fe8`，Apache-2.0，26 个包内文件，无安装生命周期脚本；
- lock 仅引用 `file:vendor/xlsx-0.20.3.tgz`；
- 正常 XLSX、损坏容器、文件/ZIP 展开量/ZIP 条目/工作表/行/单元格超限测试通过；
- 路径逃逸、符号链接/联接点、重复读取和失败后保留先前结果测试通过；
- Node `24.19.0` 下完整测试、构建和隐私扫描通过；
- `npm audit --omit=dev` 为 0 漏洞；审计期间新出现的 `browserslist@4.28.5` 告警已在既有兼容范围内升级并清除；
- 能力矩阵继续标明这是“旧版受控读取”，而不是通用上传/生产导入服务。

## 5. 来源

- [SheetJS 官方 Node.js 安装说明](https://docs.sheetjs.com/docs/getting-started/installation/nodejs/)
- [SheetJS 官方获取与版本说明](https://docs.sheetjs.com/docs/getting-started/)
- [SheetJS 原型污染公告与修复版本](https://github.com/SheetJS/sheetjs/issues/2822)
