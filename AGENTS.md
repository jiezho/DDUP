# 个人上下文智能工作台协作规则

## 项目范围

- 产品、架构和实施文档位于 `product/`。
- 调研、来源与竞品截图位于 `research/`。
- 可运行基线和目标应用位于 `person_dashboard-main/Workbench/`。
- 虚构示例知识库位于 `person_dashboard-main/个人知识库/`。
- 修改子目录前继续遵守该目录中更具体的 `AGENTS.md`；子目录规则优先。

## 产品与事实边界

- 产品定位是本地优先、来源可追溯、权限感知的个人上下文智能工作台。
- 项目工作台是通用业务底座；科研、AI 应用、学习提升等是项目模板和专业视图，不复制项目、任务或知识真源。
- Workbench 持有项目、知识、权限、审批和审计真源。一个 Run 只能有一个主 Runtime。
- DeepSeek Harness 当前是隔离只读 POC 候选；Hermes 当前是可选运行时/消息网关候选。除非有代码、测试和运行证据，不得描述为已集成或生产可用。
- 原型中的合成数据、前端状态和模拟 AI 行为不得描述为真实持久化、真实权限或真实模型能力。

## 隐私与安全

- 只使用明确标记的虚构演示数据。不得把真实个人、公司、账号、消息、浏览历史、凭据、密钥或本地私人路径写入仓库、测试、截图或报告。
- 缺失数据保持缺失，不用看似真实的数字、来源或 AI 结果填补。
- 外部写入、发送、发布、删除、权限变更、不可逆迁移、真实数据接入和持续付费服务必须先得到用户确认。
- 默认保持本地回环访问。不得无批准把本地读取或修改接口暴露到局域网或公网。
- Runtime 只能通过 Workbench Tool Gateway 访问业务能力；运行时内部记忆只能产生待确认候选，不能直接成为长期知识。

## 文档规则

- 产品或架构含义发生变化时，同步更新上位需求、UI/功能 Spec、实施计划和相关 ADR，避免多个真源。
- 需要用户决定的文档使用 `_待确认` 后缀，并在文首列出编号、推荐默认项、替代方案和不确认的影响。
- 用户确认后移除 `_待确认` 后缀，记录确认日期和最终决定。
- 文档区分“已实现、原型演示、设计方案、POC 候选和后续计划”。
- 重要结论提供代码、测试、截图或来源证据；不把推断写成事实。

## 开发规则

- 修改前先阅读相关需求、Spec、相邻代码和测试；保留用户已有改动。
- 优先完成小型端到端垂直切片，避免同时铺开多个只有静态页面的模块。
- 使用稳定领域对象和契约隔离 UI、存储、检索、模型和 Runtime。
- 新增生产依赖前说明目的、维护状态、许可证、体积、安全和可替代性，并等待确认。
- 所有写操作考虑权限、审计、幂等、撤销/补偿和失败状态。
- 正式 UI 的正文最小字号、键盘操作、焦点管理、响应式和对比度必须满足 Spec，不沿用原型中已记录的小字号和 Dialog 缺陷。

## 验证要求

修改 `person_dashboard-main/Workbench/` 后，根据影响范围执行目标测试；发布门至少执行：

```text
cd person_dashboard-main/Workbench
npm test
npm run build
npm run privacy:scan
```

- 页面或交互变化需要实际打开页面走查关键流程，并在需要时保存截图。
- 每次交付代码更新时，启动或验证本地回环试运行，并在交付信息中给出实际可访问地址；端口变化时报告实际端口，不得为方便访问改成局域网或公网监听。
- 数据模型、权限、检索和 Runtime 变化必须有正常、错误、越权、重复请求和恢复测试。
- 测试失败时记录实际失败和环境限制，不把未执行或被跳过的检查报告为通过。

## 完成定义

- 实现或文档满足明确验收标准；
- 相关测试/构建/隐私检查已执行并记录；
- 没有引入真实敏感数据或虚假生产能力描述；
- 需求、实现、测试和文档保持一致；
- 风险、限制、待确认项和后续任务已明确交接。
